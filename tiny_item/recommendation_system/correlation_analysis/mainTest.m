% buy_item = RemoveItem(training_data,user_id) ;
% [itemSet new_itemSet] = Apriori(buy_item, brand_id);
% buy_number = buyNumber(training_data,user_id,3) ;
% guess_result = RecmandToUser(sim, buy_number, buy_item, user_id) ;
% [f1 bb bp hb] = CalF1(guess_result, user_buy) ;
[boolBuy new_brand_id] = CalBoolBuy(training_data, brand_id, user_id);
b2bsim = SupportAndConfidence(boolBuy) ;
% buy_number = buyNumber(training_data,user_id,3) ;
% guess_result = GuessResult(buy_item, buy_number, b2bsim, new_brand_id, user_id) ;
% [f1 bb pb hb] = CalF1(guess_result, user_buy) ;